package io.lionpa.nok.validation.validator

import io.lionpa.nok.validation.Schema


// Если компилятор видит использование этой функции, то он заменяет её на сгенерированную функцию валидации
// TODO
fun Schema<*>.validate(){ }

// TODO
fun <T> Schema<T>.default(value: T){ }